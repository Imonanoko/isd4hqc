# isd4hqc

Information Set Decoding (ISD) algorithms for analyzing and attacking the HQC (Hamming Quasi-Cyclic) post-quantum cryptosystem — implemented in Rust.

## Usage

### Build
```bash
# build the crate
cargo build
# build in release mode
cargo build --release
```
### Run an example
```bash
cargo run --example <name>
```

## License
Apache-2.0